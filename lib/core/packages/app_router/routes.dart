abstract class AppRoutes {
  static const folders = '/folders';
  static const folderDetail = '/folders/:id';
  static const noteDetail = '/folders/:id/note/:noteId';
  static const settings = '/settings';
}
